import 'package:flutter/material.dart';

class LoadingShow extends StatelessWidget {
  const LoadingShow({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
